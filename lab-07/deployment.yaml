apiVersion: apps/v1
kind: Deployment
metadata:
  name: gps-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gps-app
  template:
    metadata:
      labels:
        app: gps-app
    spec:
      containers:
      - name: gps-container
        image: gps-app
        imagePullPolicy: Never
        ports:
        - containerPort: 3000